mixin welcome-box(title, subtitle, descr)
  +b.welcome-box_theme_dark&attributes(attributes)
    +e.content
      +e.H1.content-title= title
      +e.H2.content-subtitle= subtitle
      +e.P.content-descr=descr
